# Electron 桌面包（profile=desktop）
# 内嵌后端默认端口见 runtime-ports.json → desktop.backend（18423）
# 与本地 dev（18421）可同机并行；由 Electron 注入 DATAWISE_API_BASE_URL / preload apiBaseUrl
#
# 后端：--spring.profiles.active=desktop（见 electron/backend-service.ts）
# DATAWISE_API_BASE_URL=http://127.0.0.1:18423

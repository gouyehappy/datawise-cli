# 生产 / Electron 打包
# 留空时：浏览器仍走相对路径；Electron file:// 默认 http://127.0.0.1:18421（见 runtime-ports.json）
VITE_API_BASE_URL=

# 或通过环境变量覆盖（无需重编译前端，preload 注入）：
# DATAWISE_API_BASE_URL=http://127.0.0.1:18421
